# logic_test.py

from test import compare_numbers

print(compare_numbers(10, 5))  # Should print: First number is bigger
print(compare_numbers(3, 7))   # Should print: Second number is bigger
print(compare_numbers(4, 4))   # Should print: Both numbers are equal